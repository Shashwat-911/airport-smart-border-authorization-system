# BMS Button Fix TODO

## Approved Plan Steps:
1. [ ] Create mock api.js for successful /api/verify-traveler/ and /api/entry/ responses.
2. [ ] Update VerificationPage.jsx: Mock onScanComplete API (set travelerId), simplify handleDecision with mock API/log success.
3. [ ] Add console.logs in EntryDecisionPanel.jsx onClick and VerificationPage handleDecision for debugging.
4. [ ] Update CommandButton.jsx if needed (ensure onClick forwarding).
5. [ ] Test full flow: upload → scan → db → decision → confirmation.
6. [ ] Complete task.

Current progress tracked here.

